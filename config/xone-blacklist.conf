# xone is the correct driver for Xbox controllers with headset audio.
# Hard-block the in-kernel xpad driver so it can never bind to the controller
# and steal it from xone. The "install" line makes modprobe silently no-op.
blacklist xpad
install xpad /bin/true
blacklist mt76x2u
