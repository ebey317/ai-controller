# Voice bridge server
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
httpx>=0.27,<1.0

# Push-to-talk / keyboard listener
pynput>=1.7,<2.0

# TTS (invoked via subprocess — install separately)
# edge-tts>=6.1   -> pip install edge-tts
# mpv              -> sudo apt install mpv

# GTK on-screen keyboard (system package — not pip)
# PyGObject        -> sudo apt install python3-gi gir1.2-gtk-3.0

# Dev / test
# pytest>=8.0
